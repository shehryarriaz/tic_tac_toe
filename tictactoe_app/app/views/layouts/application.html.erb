<!DOCTYPE html>
<html>
<head>
  <title>Tic Tac Toe</title>
  <meta name="viewport" content="width=device">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="row">
    <div class="columns small-12"></div>
      <nav class="top-bar">
        <ul class="title-area">
          <li class="name">
            <h1><%= link_to "Tic Tac Toe", root_path %></a></h1>
          </li>
        </ul>
        <section class="top-bar-section">
          <ul class="right">
            <li class="divider"></li>
            <li><%= link_to "Games", games_path %></li>
            <li class="divider"></li>
            <li><%= link_to "Log in", new_session_path unless current_user %></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", logout_path if current_user %></li>
            <li class="divider"></li>
          </ul>
        </section>
      </nav>
      <% if current_user %>
        <p class="panel">Logged in as <%= current_user.email %></p>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="columns small-12"></div>
      <% flash.each do |name, msg| %>
        <div class="alert-box info radius-<%= name %>">
          <%= msg %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="columns small-12"></div>
      <%= yield %>
    </div>
  </div>

</body>
</html>
