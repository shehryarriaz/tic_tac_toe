<%= form_for @game do |f| %>
  <%= f.hidden_field :player_1_id, :value => current_user.id %>
  <%= f.label :player_2_id %>
  <%= f.collection_select :player_2_id, User.all, :id, :name %>
  <%= f.submit 'Save' %>
<% end %>